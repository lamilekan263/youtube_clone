import React from 'react';
import { Paper, Typography } from '@material-ui/core'

const VideoDetails = ({ video }) =>{
    return(
        <div>
            <h1>Thus is the video Details</h1>
        </div>
    )
}


export default VideoDetails